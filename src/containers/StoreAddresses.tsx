import React from "react";
import AddressesCards from "../components/UI/AddressesCards";

const StoreAddresses = () => {
  return (
    <div>
      <h2 className="my-10 text-2xl font-bold">Адреса магазинов</h2>
      <AddressesCards />
    </div>
  );
};

export default StoreAddresses;
